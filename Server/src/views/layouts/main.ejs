<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login NodeJS</title>
</head>
<body>
  <%if (signupMessage) {%>
    <p><%= signupMessage %></p>
  <% } %>
  <%if (signinMessage) {%>
    <p><%= signinMessage %></p>
  <% } %>

  <%- body %>
</body>
</html>